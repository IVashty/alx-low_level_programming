#include "main.h"

/**
 * _isdigit - function checks for digit(0 through 9)
 * @c: int number argument
 * Return:1 if c is a digit otherwise return 0
 *
 */

int _isdigit(int c)
{
return (c >= 48 && c <= 49);

return (0);
}
